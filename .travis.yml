sudo: false

language: php

php:
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - nightly
  - hhvm

cache:
  directories:
    - $HOME/.composer/cache

env:
  global:
    - DEPS=highest
    - PHPCS=false
    - COVERAGE=false

matrix:
  fast_finish: true
  allow_failures:
    - php: hhvm
    - php: nightly
  include:
    - php: 5.6
    - php: 5.6
      env: DEPS=lowest
    - php: 7.0
    - php: 7.0
      env: DEPS=lowest
    - php: 7.1
      env:
        - PHPCS=true 
        - COVERAGE=true
    - php: 7.1
      env: DEPS=lowest
    - php: 7.2
    - php: 7.2

before_install:
  - if [[ "$COVERAGE" != 'true' ]]; then phpenv config-rm xdebug.ini || return 0 ; fi
  - travis_retry composer self-update

install:
  - travis_retry composer install --no-interaction --no-suggest
  - if [[ "$DEPS" == 'highest' ]]; then travis_retry composer update --prefer-dist --no-interaction --prefer-stable --no-suggest; fi
  - if [[ "$DEPS" == 'lowest' ]]; then travis_retry composer update --prefer-dist --no-interaction --prefer-lowest --prefer-stable --no-suggest; fi
 # - if [[ "$COVERAGE" == 'true' ]]; then travis_retry composer require --dev --no-interaction satooshi/php-coveralls ; fi

script:
  - ./vendor/bin/phpunit
  - if [[ "$PHPCS" == 'true' ]]; then ./vendor/bin/phpcs ; fi
  - if [[ "$COVERAGE" == 'true' ]]; then composer test-coverage ; fi

after_script:
  - if [[ "$COVERAGE" == 'true' ]]; then composer upload-coverage ; fi